server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: 
          search-paths: 
          username: 
          password: 
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
